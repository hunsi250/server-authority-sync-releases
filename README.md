# Server Authority Sync releases

This public repository contains only the compiled Obsidian plugin release metadata and binaries.

Install and update on desktop and mobile with BRAT using:

`hunsi250/server-authority-sync-releases`

No source code, credentials, server configuration, or vault content is published here.
